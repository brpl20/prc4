<div class="col-md-12 col-sm-12">
  <div class="x_panel">

    <div class="x_title">
      <h2>Cadastrando novo work</h2>
      <div class="clearfix"></div>
    </div>

    <div class="x_content">

      <%= form_with model:@work do |f| %>

        <div class="col-md-12">

          <div class="col-md-4 grid">
            <b><%= f.label "Procedimento" %></b>
            <%= f.collection_check_boxes :procedure, options_for_procedure, :second, :first do |cb|
              cb.label { cb.check_box + cb.text }
            end %>
          </div>

          <div class="col-md-6">
            <p><small class="rmk-text">* Escolha o Procedimento, se for um procedimento administrativo e judicial já deixe tudo selecionado. Mas isso é tudo como um trabalho só. Se forem multiplos trabalhos recriar o formulário como um trabalho novo.</small></p>
          </div>

        </div>

        <div class="col-md-12">

          <fieldset class="col-md-4 radio_buttons optional work_subject grid">
            <legend class="col-form-label pt-0"><b>Assunto</b></legend>

            <%= f.collection_radio_buttons :subject, options_for_subject, :second, :first do |b|
              b.label { b.radio_button(class:"subject") + b.text }
            end %>

            <p><small class="rmk-text">* Escolha a área e depois a subárea de atuação.</small></p>
          </fieldset>

          <div class="conditional subject-matter sub col-md-8" data-cond-option="'work[subject]'" data-cond-value="Previdenciário">

            <fieldset class="radio_buttons optional work_subject grid">
              <legend class="col-form-label pt-0"><b>Previdenciário-Áreas</b></legend>

              <%= f.collection_radio_buttons :action, options_for_action_previdence, :second, :first do |b|
                b.label { b.radio_button + b.text }
              end %>
            </fieldset>

          </div>

          <div class="conditional subject-matter sub col-md-8" data-cond-option="'work[subject]'" data-cond-value="Cível">

            <fieldset class="radio_buttons optional work_subject grid">
              <legend class="col-form-label"><b>Cível-Áreas</b></legend>

              <%= f.collection_radio_buttons :action, options_for_action_civil, :second, :first do |b|
                b.label { b.radio_button + b.text }
              end %>
            </fieldset>

          </div>

          </div>

        </div>

        <div class="col-md-12">

          <div class="col-md-6">
            <b><%= f.label "Número do Requerimento ou Processo" %></b>
            <%= f.number_field :number, class:"form-control" %>
            <p><small class="rmk-text">* Apenas para casos em que já existe o processo.</small></p>
          </div>

        </div>

        <div class="col-md-12">

          <div class="col-md-6 grid" id="conds-rates">
            <b><%= f.label "Honorários De Trabalho ou Êxito" %></b>
            <%= f.collection_radio_buttons :rate_work, options_for_rate_work, :second, :first do |b|
            b.label { b.radio_button + b.text }
            end %>
          </div>

          <div class="conditional col-md-6" data-cond-option="'work[rate_work]'" data-cond-value="Trabalho">
            <div class="form-group">
              <b><%= f.label "Valor de Honorários Fixos"%></b>
              <%= f.text_field :rate_fixed_exfield, class:"form-control"%>
            </div>
          </div>

          <div class="conditional col-md-6" data-cond-option="'work[rate_work]'" data-cond-value="Êxito">
            <div class="form-group">
              <b><%= f.label "Valor de Honorários Percentuais" %></b>
              <%= f.text_field :rate_percentage_exfield, class:"form-control"%>
            </div>
          </div>

          <div class="conditional col-md-6" data-cond-option="'work[rate_work]'" data-cond-value="Ambos">
            <div class="form-group">

              <b><%= f.label "Valor de Honorários Fixos" %></b>
              <%= f.number_field :rate_fixed_exfield, class:"form-control"%>

              <b><%= f.label "Valor de Honorários Percentuais" %></b>
              <%= f.number_field :rate_percentage_exfield, class:"form-control"%>

            </div>
          </div>
        </div>

        <div class="col-md-12">

          <div class="grid col-md-6" id="conds-rates">
            <b><%= f.label "Parcelamento" %></b>
            <%= f.collection_radio_buttons :rate_parceled, options_for_rate_parceled, :second, :first do |b|
              b.label { b.radio_button + b.text }
            end %>
          </div>

          <div class="grid col-md-6" id="conds-rates">
            <%= f.collection_check_boxes :power, options_for_check_boxes, :second, :first do |cb|
              cb.label { cb.check_box(class:"power-checked") + cb.text }
            end %>
          </div>

        </div>

        <div class="col-md-12">

          <div class="col-md-6">
            <b><%= f.label "Escritório ou Escritórios Associados" %></b>
            <%#= f.collection_select :office, Office.order(:name), :id, :name, {}, {class:'form-control'} %>
          </div>

          <div class="col-md-6">
            <b><%= f.label "Indicação" %></b>
            <%= f.text_field :recommendation, class:"form-control" %>
          </div>

        </div>
        <%#= TODO f.input :folder, label: "Pasta #{ultimoplusone}", disabled: true, wrapper_html: {class: 'd-inline-flex'} %>
        <%#= TODO f.input :outros_responsavel %>

              <%#= TODO f.input :office, disabled: true %>
              <%#= TODO f.input :folder, label: "Pasta #{ultimoplusone}", disabled: true, wrapper_html: {class: 'd-inline-flex'} %>
              <%#= TODO f.input :outros_responsavel %>

        <div class="col-md-12">

          <div class="col-md-6">
            <b><%= f.label "Comissão" %></b>
            <%= f.text_field :recommendation_comission, class:"form-control" %>
          </div>

            <div class="col-md-6">
              <%= f.fields_for :client_works do |cw| %>

                <b><%= cw.label "Cliente" %></b>
                <%= cw.collection_select :client_id, options_for_client, :first, :second, {}, {class:'form-control'} %>

              <% end %>
            </div>

        </div>

        <div class="col-md-12">

          <div class="col-md-6">
            <b><%= f.label "Atendimento Inicial" %></b>
            <%= f.collection_select :initial_atendee, User.order(:email), :id, :email, {}, {class:'form-control'} %>
          </div>

          <div class="col-md-6">
            <b><%= f.label "Advogado Responsável" %></b>
            <%= f.collection_select :responsible_lawyer, Lawyer.order(:name), :id, :name, {}, {class:"form-control"} %>
          </div>

        </div>

        <div class="col-md-12">

          <div class="col-md-6 grid">
            <b><%= f.label "Documentos a serem produzidos * Não está funcionando ainda" %></b>
            <%= f.collection_check_boxes :checklist, options_for_checklist, :second, :first do |cl|
              cl.label { cl.check_box + cl.text }
            end %>
          </div>

          <div class="col-md-6 grid">
            <b><%= f.label "Documentos Pendentes" %></b>
            <%= f.collection_check_boxes :checklist_document, options_for_checklist_document, :second, :first do |cl|
              cl.label { cl.check_box + cl.text }
            end %>
          </div>

        </div>

        <div class="col-md-12">

          <div class="col-md-6">
            <b><%= f.label "Estagiários da Procuração" %></b>
            <%#= f.collection_select :procuration_intern, Person.order(:first_name), :id, :first_name, {}, {class:"form-control"} %>


          </div>

          <div class="col-md-6">
            <b><%= f.label "Bacharéis/Paralegais da Procuracão" %></b>
            <%#= f.text_field :procuration_paralegal, class:"form-control" %>
          </div>

        </div>

        <div class="col-md-12">

          <div class="col-md-6">
            <b><%= f.label "Advogado Parceiro" %></b>
            <%= f.text_field :partner_lawyer, class:"form-control" %>
            <p><small class="rmk-text">Caso a procuração seja outorgada também a um advogado parceiro do escritório.</small></p>
          </div>

          <div class="col-md-6">
            <b><%= f.label "Outros Documentos Pendentes ou Pendências." %></b>
            <%= f.text_field :document_pendent, class:"form-control" %>
          </div>

        </div>

        <div class="col-md-12">
          <div class="col-md-12">
            <b><%= f.label "Notas em geral sobre o caso" %></b>
            <%= f.text_area(:note, rows: 5, class:"form-control") %>
          </div>
        </div>

        <div class="form-group">
          <span>
            TESTE: <p>

            </p>
            FIM TESTE
          </span>
        </div>


        <div class="col-md-12">
          <button type="submit" class="btn btn-primary pull-right">Criar Trabalho</button>
          <%= link_to "Cancelar", works_path, class:"btn btn-danger pull-right" %>
        </div>

      <% end %>
    </div>
  </div>
</div>
